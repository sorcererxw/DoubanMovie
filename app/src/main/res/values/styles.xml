<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorCard">#fff</item>
        <item name="android:textColorPrimary">@color/md_grey_900</item>
        <item name="android:textColorSecondary">@color/md_grey_700</item>
        <!--<item name="android:windowTranslucentStatus">true</item>-->
        <!--<item name="android:windowActionBarOverlay">true</item>-->
        <!--<item name="android:windowContentOverlay">@null</item>-->
        <!--<item name="android:textColorPrimary">@android:color/white</item>-->
    </style>

    <style name="AppTheme.MainActivity" parent="AppTheme">
        <item name="android:windowBackground">@drawable/bg_starting_window</item>
    </style>

    <style name="DrawerLayoutTheme" parent="AppTheme">
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="RatingBar.Small" parent="android:style/Widget.Material.RatingBar.Small">
        <item name="android:minHeight">10dip</item>
        <item name="android:maxHeight">10dip</item>
    </style>

</resources>
